# expenses-tracker
This Python script is a CLI Personal Expense Tracker that manages spending via an interactive loop. It uses a list of dictionaries to record expense names and amounts, allowing users to add entries, view all items, calculate total spending with sum(), find the largest purchase using max(), and exit the program.
